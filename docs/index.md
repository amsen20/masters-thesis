# Linear capturing types is all you need (for static memory management)

This thesis proposes `imem`, a static memory management library for Scala using linear and capturing types, to address issues related to garbage collector pauses in long-running applications.
