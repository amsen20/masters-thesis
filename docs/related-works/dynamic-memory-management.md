# Dynamic memory management

LESS DETAILS ON THE DYNAMIC APPROACH

## Manual

## Garbage collectors

Different JVM GCs, Scala native supported GCs

## Reference counting

Python, Rust `RC` and `ARC`, `C++`'s `shared_ptr`.

## Constraint references

`Rust`'s `Cell`, `C++`'s `unique_ptr`.

## Pointer augmentation

Fat pointers and hardware assisted (CHERI) pointer augmentation.
