# Linear types

This part explains the linear type rule, which states that a linear variable should only be used once in all possible program flows. Then it elaborates the concept using an example in [[Scinear]] (the Scala implementation of the linear types).

## Main linearity rule

<!-- TODO: -->